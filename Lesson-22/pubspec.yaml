name: simple_app
description: A complex todo list app using Flutter
version: 1.0.0

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^5.0.0

  # Add any additional dependencies you might need
  # For example, if you want to use provider for state management:
  # provider: ^5.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/images/

    # Add any assets (images, fonts, etc.) you want to include
    # assets/images/
